/* global require: false, suite: false, setup: false, test: false,
    teardown: false, suiteSetup: false, suiteTeardown: false */
/* eslint strict: [1, "global"] */
/* eslint-env es6 */

'use strict';

const assert = require('assert');

suite('dummy', function() {

  suiteSetup((done) => done());


  test('example', () => {

  });


  suiteTeardown((done) => done());
});
